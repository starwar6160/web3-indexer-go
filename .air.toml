root = "."
tmp_dir = "tmp"

[build]
  bin = "./tmp/main"
  cmd = "go build -o ./tmp/main ./cmd/indexer"
  delay = 500
  exclude_dir = ["assets", "tmp", "vendor", "bin", "venv", ".git"]
  exclude_file = []
  exclude_regex = ["_test.go"]
  exclude_unchanged = false
  follow_symlink = false
  full_bin = ""
  include_dir = ["cmd", "internal", "migrations"]
  include_ext = ["go", "tpl", "tmpl", "html", "js", "css", "sql"]
  kill_delay = "0s"
  log = "build-errors.log"
  send_interrupt = false
  stop_on_error = true

[log]
  time = false

[color]
  main = "magenta"
  watcher = "cyan"
  build = "yellow"
  runner = "green"

[misc]
  clean_on_exit = true

[screen]
  clear_on_rebuild = true

# 💡 核心：注入开发环境变量
[run]
  # 💡 强力注入：在启动命令中直接显式指定环境变量，防止 air 注入失效
  full_bin = "DATABASE_URL=postgres://postgres:postgres@localhost:15432/web3_indexer?sslmode=disable RPC_URLS=http://localhost:8545 EMULATOR_ENABLED=true EMULATOR_TX_INTERVAL=500ms LOG_LEVEL=debug CONTINUOUS_MODE=true DEMO_MODE=true ./tmp/main --reset"
