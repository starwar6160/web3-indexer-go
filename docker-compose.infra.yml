services:
  # PostgreSQL 数据库
  postgres:
    image: postgres:15-alpine
    container_name: web3-indexer-db
    restart: always
    environment:
      POSTGRES_DB: web3_indexer
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "15432:5432"
    volumes:
      - indexer_db_data:/var/lib/postgresql/data
      - ./scripts/init-db.sql:/docker-entrypoint-initdb.d/init-db.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d web3_indexer"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - indexer-network

  # Anvil 本地以太坊节点
  anvil:
    image: ghcr.io/foundry-rs/foundry:latest
    container_name: web3-indexer-anvil
    restart: always
    network_mode: host
    entrypoint: ["anvil"]
    command: ["--host", "127.0.0.1", "--block-time", "1", "--chain-id", "31337", "--accounts", "10"]
    volumes:
      - indexer_anvil_data:/root/.foundry/anvil
    environment:
      - FOUNDRY_DISABLE_NIGHTLY_WARNING=true
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  indexer_db_data:
  indexer_anvil_data:

networks:
  indexer-network:
    driver: bridge
