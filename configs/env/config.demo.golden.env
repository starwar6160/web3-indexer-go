# ============================================================================
# ANVIL DEMO INSTANCE CONFIGURATION (PORT 8082)
# ============================================================================

# Local Anvil RPC (Changed from 'anvil' to 'localhost' for Host Mode)
RPC_URLS="http://localhost:8545"
CHAIN_ID=31337

# Start from genesis for local demo
START_BLOCK=0

# High performance settings for local Anvil
DEMO_MODE=true
IS_TESTNET=false
MAX_SYNC_BATCH=100
RPC_RATE_LIMIT=200
FETCH_CONCURRENCY=10

# Database configuration for Demo indexer (Use localhost:5432)
DATABASE_URL="postgres://postgres:uWNZugjBqixf8dxC@localhost:5432/web3_demo?sslmode=disable"

# API Port (Internal)
API_PORT=8082
PORT=8082

# Energy saving disabled for stable demo
TOKEN_FILTER_MODE=all
ENABLE_ENERGY_SAVING=false
LOG_LEVEL=info

# Port Configuration
DB_PORT=15434
EXTERNAL_PORT=8082
POSTGRES_DB=web3_indexer

# UI Title (Quoted for Shell compatibility)
APP_TITLE="🛠️ WEB3 INDEXER - LOCAL STABLE (8082)"
