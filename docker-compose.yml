services:
  # Web3 Indexer Application
  indexer:
    image: web3-indexer-go:stable
    container_name: ${COMPOSE_PROJECT_NAME}-app
    deploy:
      resources:
        limits:
          memory: 128M
    network_mode: host  # 👈 核心修改：使用 Host 模式，彻底打通 Tailscale 和宿主机端口
    environment:
      # 直接连接宿主机的基础设施数据库 (15432)
      - DATABASE_URL=${DATABASE_URL:-postgres://postgres:W3b3_Idx_Secur3_2026_Sec@localhost:15432/web3_indexer?sslmode=disable}
      - RPC_URLS=${RPC_URLS}
      - CHAIN_ID=${CHAIN_ID}
      - START_BLOCK=${START_BLOCK}
      - DEMO_MODE=${DEMO_MODE}
      - IS_TESTNET=${IS_TESTNET}
      - ENABLE_ENERGY_SAVING=${ENABLE_ENERGY_SAVING}
      - APP_TITLE=${APP_TITLE:-🚀 Web3 Indexer (Default)}
      - PORT=${PORT:-8080}  # 使用环境变量中的端口
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:${PORT:-8080}/api/status"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 10s
    restart: always

  # Anvil Service (Only for Demo Mode)
  anvil:
    image: ghcr.io/foundry-rs/foundry:latest
    container_name: ${COMPOSE_PROJECT_NAME}-anvil
    profiles: ["demo"]
    network_mode: host # 也可以改为 host
    entrypoint: ["anvil"]
    command: ["--host", "0.0.0.0", "--port", "8545", "--block-time", "1"]
    restart: always

# 移除独立的 db 网络定义，因为我们现在复用宿主机的基础设施